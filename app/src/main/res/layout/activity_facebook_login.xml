<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:facebook="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/login_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:textColor="@color/text_color_white"
        android:text="@string/facebook_login_text"
        android:padding="@dimen/login_btn_padding"
        android:layout_centerInParent="true" />

    <com.facebook.login.widget.LoginButton
        android:visibility="gone"
        android:id="@+id/facebook_login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="@dimen/facebook_login_button_margin"
        android:layout_marginTop="@dimen/facebook_login_button_margin" />

</RelativeLayout>
